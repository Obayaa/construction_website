export default function Section_Title({ title }) {
    return (
        <h1
            className='text-5xl text-center font-bold text-blue-800 underline underline-offset-[16px] decoration-red-600'>
            {title}
        </h1>
    )
}